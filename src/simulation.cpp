#include <iostream>
#include <simulation.h>

using namespace std;

namespace vio_init
{
    SimulationVio::SimulationVio(const Waypoints &w_pts,
                                 const VioSimulationParams &vioSimParams)
    {
        cout << "Initialized" << endl;
    }

}